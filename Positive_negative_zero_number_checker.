
#include <stdio.h> 
  
int main() 
{ 
    int input; 
  
    printf("Enter the number : "); 
    scanf("%d", &input); 
  
    if (A > 0) 
        printf("%d is positive.", intput); 
    else if (A < 0) 
        printf("%d is negative.", input); 
    else if (A == 0) 
        printf("%d is zero.", input); 
  
    return 0; 
}
